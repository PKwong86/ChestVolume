% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Vol_overtime.R,
%   R/calculate_convex_hull_volume.R
\name{calculate_segment_volumes}
\alias{calculate_segment_volumes}
\title{Calculate Segment Volumes Over Time}
\usage{
calculate_segment_volumes(data, segments, unit_conversion = 0.1)

calculate_segment_volumes(data, segments, unit_conversion = 0.1)
}
\arguments{
\item{data}{A data frame containing adjusted marker coordinates, with columns 'Timeframe', 'Marker', 'X', 'Y', 'Z'.}

\item{segments}{A list of character vectors, each containing marker names defining a segment.}

\item{unit_conversion}{Numeric value used to convert the units of the coordinates (default is 0.1, converting mm to cm).}

\item{adjust_markers}{Logical value indicating whether to adjust markers for protrusion (default is TRUE).}

\item{adjustment_distance}{Numeric value for marker adjustment distance (default is 1 cm).}
}
\value{
A list of numeric vectors, each containing volumes for a segment over time.

A data frame with columns 'Timeframe', 'Segment', 'Volume'.
}
\description{
Calculates the volumes of specified segments over time using convex hulls.

Calculates the volumes of specified segments over time using convex hulls, with an option to convert coordinate units.
}
\details{
The function multiplies the coordinates by \code{unit_conversion} to convert units (e.g., from mm to cm) before calculating volumes.
}
\examples{
# Define segments (e.g., quadrants of the chest)
segments <- list(
  UL = c("M01", "M02", "M03", "M04"),
  UR = c("M05", "M06", "M07", "M08")
)

# Assume 'adjusted_data' is the data frame with adjusted marker positions
# Coordinates are in mm; we'll convert to cm by default
volumes_df <- calculate_segment_volumes(adjusted_data, segments)
head(volumes_df)

# If coordinates are already in cm, set unit_conversion to 1
volumes_df_cm <- calculate_segment_volumes(adjusted_data, segments, unit_conversion = 1)

}
